export enum DepsManager {
  Inversify = "inversify",
}

export enum Servers {
  Express = "express",
}

export enum DatabaseClients {
  Mysql = "sql",
  Mongo = "mongo",
}

export enum MessagingClients {
  Rabbit = "rabbit",
}